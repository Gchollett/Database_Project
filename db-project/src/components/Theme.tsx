import { Theme, createTheme } from '@mui/material/styles';

export const theme: Theme = createTheme({
  palette: {
    primary: {
      light: '#172531',
      main: '#213547',
      dark: '#4d5d6b',
      contrastText: '#fff',
    }
  },
});